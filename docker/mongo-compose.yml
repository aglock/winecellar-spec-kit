services:
  mongo:
    image: mongo:8
    container_name: winecellar-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: winecellar
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
